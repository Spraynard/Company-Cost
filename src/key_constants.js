/**
 * Constants used for defining keyboard actions.
 * Thought it would be a little more readable then just saying
 * e.which === 32
 */
const key_constants = {
	"BACKSPACE_KEY" : 8,
	"DELETE_KEY" : 46,
	"ENTER_KEY" : 13,
	"ESCAPE_KEY" : 27,
	"SHIFT_KEY" : 16,
	"SPACE_KEY" : 32,
	"TAB_KEY" : 9,
};

module.exports = key_constants;